<!-- 新規投稿　あとでメインと結合 -->
<h1>New book</h1>
<!-- form_for(入力欄) -->
<%= form_for(@post) do |f| %>
	<p>Title</p>
	<%= f.text_field :title %>

	<p>Body</p>
	<%= f.text_area :body %>

	<%= f.submit 'Create Book' %>
<% end %>